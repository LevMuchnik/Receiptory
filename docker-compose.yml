services:
  receiptory:
    build: .
    ports:
      - "${RECEIPTORY_PORT:-8484}:${RECEIPTORY_PORT:-8484}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - RECEIPTORY_DATA_DIR=/app/data
